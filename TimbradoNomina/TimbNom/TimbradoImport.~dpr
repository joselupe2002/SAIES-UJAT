program TimbradoImport;

uses
  Forms,
  UPrincipal in 'UPrincipal.pas' {FPrincipal},
  Uentrada in 'Uentrada.pas' {entrada};

{$R *.RES}

begin
  Application.Initialize;
  Application.CreateForm(TFPrincipal, FPrincipal);
  Application.CreateForm(Tentrada, entrada);
  Application.Run;
end.
